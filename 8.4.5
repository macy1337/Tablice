#include <iostream>
#include <string>
using namespace std;

void generuj(string T[], int n)
{
    for (int i = 0; i < n; i++)
    {
        cout << "T[" << i << "]=";
        cin >> T[i];
    }
}

bool parzyste(string liczba)
{
    for (int i = 0; i < liczba.size(); i++)
    {
        int t = liczba[i] - 48;
        if(t % 2 != 0) return false;
    }
    return true;
}

void pokaz(string T[], int n)
{
    for (int i = 0; i < n; i++)
    {
        if (parzyste(T[i]) == true)
        {
            cout << "T[" << i << "]=" << T[i] << " ";
        }
    }
}

int main()
{
    string T[1000]; 
    int n;
    cout << "Podaj n: ";
    cin >> n;
    generuj(T, n);
    pokaz(T, n);
    return 0;
}
