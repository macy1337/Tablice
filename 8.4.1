#include <iostream>
using namespace std;

void generuj(int T[], int n)
{
    for (int i = 0; i < n; i++)
    {
        cout << "T[" << i << "]=";
        cin >> T[i];
    }
}

void pokaz(int T[], int n)
{
    for (int i = 0; i < n; i++)
    {
        if (T[i] % 2 == 0)
        {
            cout << "T[" << i << "]=" << T[i] << " ";
        }
    }
}

int main()
{
    int T[1000];
    int n;
    cout << "Podaj n: ";
    cin >> n;
    generuj(T, n);
    pokaz(T, n);
    return 0;
}
