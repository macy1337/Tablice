#include <iostream>
#include <string>
using namespace std;

void zamien(int T[], string x)
{
    for (int i = 0; i < x.size(); i++)
    {
        T[100 - x.size() + i] = x[i] - 48;
    }
}

int main()
{
    int T[100] = {0};
    string x = "123";
    zamien(T, x);
}
